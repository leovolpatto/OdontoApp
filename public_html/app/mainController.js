app.controller("MainController", function($scope){
    
    /*
    $scope.menuAtivo = {};
    
    $scope.menus = [
        {
            nome : "Pessoas",
            id : "menuPessoas",
            ativo : true
        },
        {
            nome : "Serviços",
            id : "menuServicos",
            ativo : false
        }
    ];
    
    $scope.selecionarMenu = function(menu){
        angular.forEach($scope.menus, function(m){
            m.ativo = m.id === menu.id;
            if(m.ativo){
                $scope.menuAtivo = m;
            }
        });
    };*/
});